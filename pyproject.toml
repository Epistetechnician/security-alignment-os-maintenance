[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "security-alignment-os"
version = "0.1.0"
description = "Claim-bounded, capability-gated reference controls for AI agents"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
saos-demo = "alignment_os.demo:main"

[tool.setuptools.packages.find]
include = ["alignment_os*"]
